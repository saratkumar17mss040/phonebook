{
	"name": "phonebookbackend",
	"version": "1.0.0",
	"description": "A simple rest-api backend for my phonebook application",
	"main": "server.js",
	"scripts": {
		"start": "node server.js",
		"dev": "nodemon server.js",
		"test": "echo \"Error: no test specified\" && exit 1",
		"build:ui": "rm -rf build && cd ../phonebookFrontend && npm run build --prod && cp -r build ../phonebook",
		"deploy": "git push heroku main",
		"deploy:full": "npm run build:ui && git add . && git commit -m uibuild & git push && npm run deploy",
		"logs:prod": "heroku logs --tail"
	},
	"keywords": [
		"rest-api",
		"phonebook-api"
	],
	"author": "sarath",
	"license": "MIT",
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.17.1",
		"morgan": "^1.10.0"
	},
	"devDependencies": {
		"nodemon": "^2.0.9"
	}
}
